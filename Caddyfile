{$HOST_URL} {
    # Route API requests to the backend server
    handle_path /api/* {
        rewrite * {uri}
        reverse_proxy server:8080
    }

    # Route everything else to the client frontend
    handle {
        reverse_proxy client:3000
    }
}
